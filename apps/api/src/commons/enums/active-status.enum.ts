/**
 * Enum for Active status
 * @readonly
 * @example ActiveStatusEnum.ACTIVE
 * @returns {number} Returns the status value (0 = inactive, 1 = active)
 */
export enum ActiveStatusEnum {
  IN_ACTIVE = 0,
  ACTIVE = 1,
}
